还没创建虚拟环境，先创建：
python -m venv venv

创建完成后再激活：
.\venv\Scripts\Activate.ps1

Windows 下进入虚拟环境：
# Git Bash / PowerShell / CMD 都可以：
cd c:\Users\lenovo\Desktop\smart-service-agent\python-impl
venv\Scripts\activate

# 4. 启动服务
python -m api.main


直接用交互模式：
mysql -u root -p123456

进去后执行：
source scripts/init_mysql.sql
SHOW TABLES;
EXIT;

部署命令
# 构建并启动
cd python-impl
docker-compose up -d

# 查看日志
docker-compose logs -f app

# 检查就绪
curl http://localhost:8000/ready

# 启动服务后运行
python evals/runner.py

# 只测意图
python evals/runner.py --suite intent

# 输出 JSON 报告
python evals/runner.py --json report.json

taskkill -f -pid 22600 2>&1 && echo "Port 8000 freed"


非流式（完整返回后打印）
python scripts/cli_chat.py

流式 SSE（打字机效果）
python scripts/cli_chat.py --stream

重启服务后再测试：
uvicorn api.main:app --reload --port 8000









